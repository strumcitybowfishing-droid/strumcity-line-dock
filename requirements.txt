# No external dependencies required.
# This file exists only to satisfy Render's default Python build step
# which runs "pip install -r requirements.txt".
# The app uses only the Python standard library.
